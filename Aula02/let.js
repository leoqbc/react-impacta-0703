// não pode ser alterada
const teste = 123;

// array
const numeros = [1, 3, 4, 5];

numeros.push(10);

// não é possível reatribuição = substituição
// numeros = 1;

console.log(numeros);

const nome = 'Leo';

if (nome === 'Leo') {
    const naoacesso = 123;
}

// console.log(naoacesso);

const contador = 1;

contador++; // contador = contador + 1

// recomandação - EcmaScript(ES6) ou NodeJS (backend) 